app:
  name: "my-application"
  version: "1.0.0"
  environment: "development"

database:
  host: "localhost"
  port: "5432"  # ❌ Error: should be number, not string
  name: "myapp"
  credentials:
    username: "admin"
    password: "hardcoded_password_123"  # ❌ Error: hardcoded secret

security:
  cors:
    origin: "https://myapp.com"  # ❌ Error: should be array
    credentials: "true"  # ❌ Error: should be boolean
  headers:
    x-frame-options: "DENY"
    # ❌ Error: missing required security headers

logging:
  level: "invalid_level"  # ❌ Error: invalid log level
  format: "json"
  output: "stdout"

# ❌ Error: incorrect YAML syntax - missing indentation
cache:
redis:
  host: "localhost"
  port: 6379
  password: "redis_password"  # ❌ Error: hardcoded secret 